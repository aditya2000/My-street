import React, {Component} from 'react';
import './invalidUrl.css';

class InvalidUrl extends Component {
    render() {
        return(
            <div className="sellerLogin">
                <h1>Route doen't exists!!</h1>
            </div>
        );
    }
}

export default InvalidUrl;